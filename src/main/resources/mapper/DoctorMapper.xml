<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="cn.bdqn.dao.DoctorMapper">
    <select id="findQueryDoctor" resultType="Doctor" parameterType="String">
        SELECT 	d.id,d.d_name,d.d_pinyin,d.birth_date,d.cy_date,d.e_address,k.k_name,d.postion,d.edcuation,d.graduate,d.hospital,d.resume,d.isExpert,d.achievement,d.photopath,d.status,d.remark
        FROM doctor d
        JOIN koffice k ON d.k_id=k.id
        <where>
            <if test="dname != null and dname != ''">
                and d.d_name LIKE "%"#{dname}"%"
            </if>

            <if test="dpinyin != null and dpinyin != ''">
                and d.d_name LIKE "%"#{dpinyin}"%"
            </if>
        </where>
    </select>


</mapper>